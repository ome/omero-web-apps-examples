
from setuptools import setup, find_packages

setup(
    name='react-webapp',
    version='0.0.1',
    description="OMERO example React web app",
    packages=find_packages(),
    keywords=['omero', 'example', 'react'],
)
